machine:
  environment:
    PATH: $HOME/.cabal/bin:$HOME/installs/bin:$PATH
  cache_directories:
    - "~/installs"
dependencies:
  cache_directories:
    - "~/installs"
  override:
    - sh ~/$CIRCLE_PROJECT_REPONAME/install_deps.sh
test:
  cache_directories:
    - "dist"
    - "~/.cabal"
  pre:
    - cabal update
    - cabal install --only-dependencies
  override:
    - cabal configure
    - cabal build
    - cabal test
  deployment:
    hackage:
      branch: hackage
      commands:
        - cabal upload -u $CABAL_USERNAME -p $CABAL_PASSWORD
